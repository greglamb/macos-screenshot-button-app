# .github/workflows/security.yml — generated by bootstrap-security.sh
name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read
  pull-requests: read

jobs:
  security:
    uses: greglamb/gha-workflows/.github/workflows/lib-security-review.yml@v0.2605.512
    permissions:
      contents: read
      security-events: write
      actions: read
      pull-requests: read